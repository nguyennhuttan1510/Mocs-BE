var Event = require("../../models/clients/Event");
var Menu = require("../../models/MenuModel");
var Table = require("../../models/TableModels");

const controllerClient = (socket, io) => {};

module.exports = controllerClient;
